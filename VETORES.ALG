Algoritmo "vetores"

Var

  funcionarios:vetor[1..4] de caractere
  contador:inteiro

Inicio

   para contador de 1 ate 4 faca
   escreval("Digite o nome do seu fúncionário: ")
   leia(funcionarios[contador])
   fimpara
   
   para contador de 1 ate 4 faca
     escreval ("indice atual ", contador, " ", funcionarios[contador])
   fimpara

Fimalgoritmo